import { createEntityNavigation } from "./createEntityNavigation";

export const AppMenuNavigation = createEntityNavigation(
  "app-menu",
  "app-menus"
);
export const TimezoneGroupNavigation = createEntityNavigation(
  "timezone-group",
  "timezone-groups"
);
