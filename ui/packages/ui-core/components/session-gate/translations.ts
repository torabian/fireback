/**
 * Translations for <SessionGate>.
 *
 * Deliberately NOT part of the auto-generated
 * `../strings/translations.ts` (that file is regenerated by the fireback
 * language & translation manager and shouldn't be hand-edited). Keeping this
 * component's strings local to its own folder is what makes it possible to
 * delete the whole `session-gate` folder and be done - nothing else needs to
 * be untangled.
 */

export const en = {
  checking: "Checking your session…",
  attempt: "Still trying to reach the server — attempt {n}…",
  cancel: "Cancel",
};

export const pl = {
  checking: "Sprawdzanie sesji…",
  attempt: "Wciąż próbuję połączyć się z serwerem — próba {n}…",
  cancel: "Anuluj",
};

export const ru = {
  checking: "Проверка сеанса…",
  attempt: "Пытаемся подключиться к серверу — попытка {n}…",
  cancel: "Отмена",
};

export const es = {
  checking: "Comprobando tu sesión…",
  attempt: "Seguimos intentando conectar con el servidor — intento {n}…",
  cancel: "Cancelar",
};

export const fa = {
  checking: "در حال بررسی نشست شما…",
  attempt: "همچنان در تلاش برای اتصال به سرور — تلاش {n}…",
  cancel: "لغو",
};

export type SessionGateStrings = typeof en;

export const sessionGateStrings: Record<string, SessionGateStrings> = {
  en,
  pl,
  ru,
  es,
  fa,
};

/** Locales this component ships copy for. */
export const SESSION_GATE_LOCALES = Object.keys(
  sessionGateStrings,
) as Array<keyof typeof sessionGateStrings>;

/** Locales that should render right-to-left. */
export const SESSION_GATE_RTL_LOCALES = new Set(["fa"]);
