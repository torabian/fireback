/*
*	Generated by fireback 1.2.2
*	Written by Ali Torabi.
* The code is generated for react-query@v3.39.3
*	Checkout the repository for licenses and contribution: https://github.com/torabian/fireback
*/
import { useContext } from "react";
import { useQuery } from "react-query";
import { 
  RemoteQueryContext,
  UseRemoteQuery,
  queryBeforeSend,
} from "../../core/react-tools";
import { stringify } from "qs";
import { execApiFn, IResponseList } from "../../core/http-tools";
    import {
        BackupTableMetaEntity,
    } from "../abac/BackupTableMetaEntity"
// Fireback gives the option to have typesafe query string
export type GetBackupTableMetasExportQs = {
}
export type GetBackupTableMetasExportUrlParams = {
}
export type GetBackupTableMetasExportHeaders = {
}
export function useGetBackupTableMetasExport({
  queryOptions,
  query,
  queryClient,
  execFnOverride,
  unauthorized,
  optionFn
}: UseRemoteQuery & {query?: GetBackupTableMetasExportQs}) {
  const { options, execFn } = useContext(RemoteQueryContext);
  const computedOptions = optionFn ? optionFn(options) : options;
  // Calculare the function which will do the remote calls.
  // We consider to use global override, this specific override, or default which
  // comes with the sdk.
  const rpcFn = execFnOverride
    ? execFnOverride(computedOptions)
    : execFn
    ? execFn(computedOptions)
    : execApiFn(computedOptions);
  // Url of the remote affix.
  const url = "/backup-table-metas/export".substr(1);
  let computedUrl = `${url}?${stringify(query)}`;
  let completeRouteUrls = true;
  // Attach the details of the request to the fn
  const fn = () => rpcFn("GET", computedUrl);
  const auth = computedOptions?.headers?.authorization
  const hasKey = auth != "undefined" && auth != undefined && auth !=null && auth != "null" && !!auth
  let enabled = true;
  if (!completeRouteUrls) {
    enabled = false;
  } else if (!hasKey && !unauthorized) {
    enabled = false;
  }
  const query$ = useQuery<any, any, IResponseList<BackupTableMetaEntity>, any>(["*abac.BackupTableMetaEntity", computedOptions, query], fn, {
    cacheTime: 1000,
    retry: false,
    keepPreviousData: true,
    enabled,
    ...((queryOptions as any) || {})
  } as any);
  const items: Array<BackupTableMetaEntity> = query$.data?.data?.items || [];
  return { 
    query: query$,
    items,
    keyExtractor: (item: BackupTableMetaEntity) => item.uniqueId,
  };
}
useGetBackupTableMetasExport.UKEY = "*abac.BackupTableMetaEntity"