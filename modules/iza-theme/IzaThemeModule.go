package izaTheme

/*
*	Generated by fireback <no value>
*	Written by Ali Torabi.
*	Checkout the repository for licenses and contribution: https://github.com/torabian/fireback
 */
import (
	"embed"

	"github.com/torabian/fireback/modules/fireback"
	"github.com/torabian/fireback/modules/iza-theme/migrations"
)

//go:embed *Module3.yml
var Module3Definitions embed.FS

//go:embed all:screens
var embeddedScreens embed.FS

type IzaThemeModuleConfig struct {
	// Add whatever you need to pass to this module for setup.
	// This is used only if you want to customize behavior of module,
	// do not use for passing environment variables, there is another mechanism for it
}

func IzaThemeModuleSetup(cfg *IzaThemeModuleConfig) *fireback.ModuleProvider {
	module := &fireback.ModuleProvider{
		Name:        "iza",
		Definitions: &Module3Definitions,
		// Adds all of the actions as cli, you can remove it and modify specific
		// actions which go to the cli.
		// if the functions does not exist, make sure you compile .yml module once at least
		ActionsBundle: GetIzaThemeActionsBundle(),
		// each entity, has multiple features, such as permissions, events, translations
		// *EntityBundle objects are a list of them which are auto generated,
		// and by adding them here it will be automatically added.
		// we cannot add them automatically upon saving yaml for you,
		// when you add a new entity in yaml, add it manually here.
		EntityBundles: []fireback.EntityBundle{
			// Insert the NameEntityBundle here.
		},
		CliHandlers: GetIzaThemeActionsCli(),
		// Goose framework directory, which would contain sql files for that framework,
		// which would be running near automigration of gorm.
		GoMigrateDirectory: &migrations.MigrationsFs,
	}
	return module
}
