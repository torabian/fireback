/**
* Action to communicate with the action createWorkspace
*/
 






	
/**
  * @decription The base class definition for createWorkspaceReq
  **/

export class CreateWorkspaceReq {
	 
		/**
  * @type {string}
  * @description 
  **/
 name;
		/**
  * @returns {string}
  * @description 
  **/
getName () { return this[`name`] }
		/**
  * @param {string}
  * @description 
  **/
setName (value) { this[`name`] = value; return this; } 
	 
		/**
  * @type {WorkspaceEntity}
  * @description 
  **/
 workspace;
		/**
  * @returns {WorkspaceEntity}
  * @description 
  **/
getWorkspace () { return this[`workspace`] }
		/**
  * @param {WorkspaceEntity}
  * @description 
  **/
setWorkspace (value) { this[`workspace`] = value; return this; } 
	 
		/**
  * @type {string}
  * @description 
  **/
 workspaceId;
		/**
  * @returns {string}
  * @description 
  **/
getWorkspaceId () { return this[`workspaceId`] }
		/**
  * @param {string}
  * @description 
  **/
setWorkspaceId (value) { this[`workspaceId`] = value; return this; } 
	

	

	/** a placeholder for WebRequesX auto patching the json content to the object **/
	static __jsonParsable;
}










/**
 * CreateWorkspaceHeaders class
 * Auto-generated from Module3Action
 */
export class CreateWorkspaceHeaders extends Headers {

  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  /**
   * @returns {Record<string, string>}
   * Converts Headers to plain object
   */
  toObject() {
    return Object.fromEntries(this.entries());
  }

  
  /**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@FetchUserHeader.Nest() headers: FetchUserHeader): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data: unknown, ctx: ExecutionContext) => {
		// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
		const request = ctx.switchToHttp().getRequest();
		// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-argument, @typescript-eslint/no-unsafe-member-access
		return new CreateWorkspaceHeaders(Object.entries(request.headers));
	},
  );

  
}




/**
 * CreateWorkspaceQueryParams class
 * Auto-generated from Module3Action
 */
export class CreateWorkspaceQueryParams extends URLSearchParamsX {


  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  
  /**
   * Nest.js decorator for controller query. Instead of using @Query() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@FetchUserQuery.Nest() query: FetchUserQuery): string {
   *  return JSON.stringify(query.getMyfield());
   * }
   */
  static Nest = createParamDecorator(
	(_data: unknown, ctx: ExecutionContext) => {
		// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
		const request = ctx.switchToHttp().getRequest();
		// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-argument, @typescript-eslint/no-unsafe-member-access
		return new CreateWorkspaceQueryParams(request.query);
	},
  );

  
}


 
