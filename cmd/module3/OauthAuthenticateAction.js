/**
* Action to communicate with the action oauthAuthenticate
*/
 






	
/**
  * @decription The base class definition for oauthAuthenticateReq
  **/

export class OauthAuthenticateReq {
	 
		/**
  * @type {string}
  * @description The token that Auth2 provider returned to the front-end, which will be used to validate the backend
  **/
 token;
		/**
  * @returns {string}
  * @description The token that Auth2 provider returned to the front-end, which will be used to validate the backend
  **/
getToken () { return this[`token`] }
		/**
  * @param {string}
  * @description The token that Auth2 provider returned to the front-end, which will be used to validate the backend
  **/
setToken (value) { this[`token`] = value; return this; } 
	 
		/**
  * @type {string}
  * @description The service name, such as 'google' which later backend will use to authorize the token and create the user.
  **/
 service;
		/**
  * @returns {string}
  * @description The service name, such as 'google' which later backend will use to authorize the token and create the user.
  **/
getService () { return this[`service`] }
		/**
  * @param {string}
  * @description The service name, such as 'google' which later backend will use to authorize the token and create the user.
  **/
setService (value) { this[`service`] = value; return this; } 
	

	

	/** a placeholder for WebRequesX auto patching the json content to the object **/
	static __jsonParsable;
}













	
/**
  * @decription The base class definition for oauthAuthenticateRes
  **/

export class OauthAuthenticateRes {
	 
		/**
  * @type {UserSessionDto}
  * @description 
  **/
 session;
		/**
  * @returns {UserSessionDto}
  * @description 
  **/
getSession () { return this[`session`] }
		/**
  * @param {UserSessionDto}
  * @description 
  **/
setSession (value) { this[`session`] = value; return this; } 
	 
		/**
  * @type {string[]}
  * @description The next possible action which is suggested.
  **/
 next;
		/**
  * @returns {string[]}
  * @description The next possible action which is suggested.
  **/
getNext () { return this[`next`] }
		/**
  * @param {string[]}
  * @description The next possible action which is suggested.
  **/
setNext (value) { this[`next`] = value; return this; } 
	

	

	/** a placeholder for WebRequesX auto patching the json content to the object **/
	static __jsonParsable;
}








/**
 * OauthAuthenticateHeaders class
 * Auto-generated from Module3Action
 */
export class OauthAuthenticateHeaders extends Headers {

  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  /**
   * @returns {Record<string, string>}
   * Converts Headers to plain object
   */
  toObject() {
    return Object.fromEntries(this.entries());
  }

  
  /**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@FetchUserHeader.Nest() headers: FetchUserHeader): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data: unknown, ctx: ExecutionContext) => {
		// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
		const request = ctx.switchToHttp().getRequest();
		// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-argument, @typescript-eslint/no-unsafe-member-access
		return new OauthAuthenticateHeaders(Object.entries(request.headers));
	},
  );

  
}




/**
 * OauthAuthenticateQueryParams class
 * Auto-generated from Module3Action
 */
export class OauthAuthenticateQueryParams extends URLSearchParamsX {


  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  
  /**
   * Nest.js decorator for controller query. Instead of using @Query() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@FetchUserQuery.Nest() query: FetchUserQuery): string {
   *  return JSON.stringify(query.getMyfield());
   * }
   */
  static Nest = createParamDecorator(
	(_data: unknown, ctx: ExecutionContext) => {
		// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment
		const request = ctx.switchToHttp().getRequest();
		// eslint-disable-next-line @typescript-eslint/no-unsafe-assignment, @typescript-eslint/no-unsafe-argument, @typescript-eslint/no-unsafe-member-access
		return new OauthAuthenticateQueryParams(request.query);
	},
  );

  
}


 
