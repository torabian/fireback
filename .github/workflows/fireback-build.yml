name: Fireback Authentication Server
env:
  CI: false
  TERM: dumb

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

on:
  push:
    branches:
      - "v**"
      - "main"
  pull_request:
    branches:
      - "v**"
      - "main"
  workflow_dispatch:
    inputs:
      release_artifacts:
        description: "Create draft release"
        required: false
        default: false
        type: boolean
      github_pages:
        description: "Deploy docs and demo to pages"
        required: false
        default: false
        type: boolean
jobs:

  deploy_github_release:
    needs:
      - build-windows
      - build-ubuntu
      - build-macos
      - build-ui-packages
    name: deploy_github_release
    if: ${{ inputs.release_artifacts == true }}
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Get the version tag
        id: get_version_tag
        run: echo "tag_name=$(basename ${GITHUB_REF})" >> $GITHUB_ENV

      - name: release
        uses: actions/create-release@v1
        id: create_release
        with:
          draft: true
          prerelease: true
          release_name: ${{ steps.version.outputs.version }}
          tag_name: ${{ env.tag_name }}
          body_path: README.md
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - uses: actions/download-artifact@master
        with:
          name: artifacts-ubuntu
          path: artifacts-ubuntu

      - uses: actions/download-artifact@master
        with:
          name: artifacts-windows
          path: artifacts-windows
      - uses: actions/download-artifact@master
        with:
          name: artifacts-darwin
          path: artifacts-darwin

      - uses: actions/download-artifact@master
        with:
          name: artifacts-ui-packages
          path: artifacts-ui-packages

      # ui/packages/* (see ui/packages/README.md) ship one npm-installable tarball
      # per package - filenames carry each package's own version
      # (fireback-sdk-0.1.0.tgz, ...) so a fixed asset_path/asset_name per file (the
      # actions/upload-release-asset@v1 pattern the binaries above use) would break the
      # moment any one package's version changes. `gh release upload` takes a glob
      # instead, so it doesn't need updating when versions move.
      - name: upload ui package tarballs
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: gh release upload ${{ env.tag_name }} artifacts-ui-packages/*.tgz --repo ${{ github.repository }}

      - name: upload mac amd64 zip artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-darwin/fireback-server-all/fireback_amd64_darwin.zip
          asset_name: fireback_amd64_darwin.zip
          asset_content_type: application/zip

      - name: upload mac arm64 zip artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-darwin/fireback-server-all/fireback_arm64_darwin.zip
          asset_name: fireback_arm64_darwin.zip
          asset_content_type: application/zip

      - name: upload mac arm64 pkg artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-darwin/fireback-server-all/fireback_silicon_arm64.pkg
          asset_name: fireback_silicon_arm64.pkg
          asset_content_type: application/x-newton-compatible-pkg
      - name: upload mac amd64 pkg artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-darwin/fireback-server-all/fireback_intel_amd64.pkg
          asset_name: fireback_intel_amd64.pkg
          asset_content_type: application/x-newton-compatible-pkg


      - name: upload windows amd64 artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-windows/fireback-server-all/fireback_win_amd64_installer.msi
          asset_name: fireback_win_amd64_installer.msi
          asset_content_type: application/x-ms-installer
      - name: upload windows arm64 artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-windows/fireback-server-all/fireback_win_arm64_installer.msi
          asset_name: fireback_win_arm64_installer.msi
          asset_content_type: application/x-ms-installer
      - name: upload linux installer arm64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-ubuntu/fireback-server-all/fireback-arm64.deb
          asset_name: fireback-arm64.deb
          asset_content_type: application/vnd.debian.binary-package
 
      - name: upload linux installer amd64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-ubuntu/fireback-server-all/fireback-amd64.deb
          asset_name: fireback-amd64.deb
          asset_content_type: application/vnd.debian.binary-package
      - name: upload linux zip amd64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-ubuntu/fireback-server-all/fireback_amd64_linux.zip
          asset_name: fireback_amd64_linux.zip
          asset_content_type: application/zip
      - name: upload linux zip arm64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-ubuntu/fireback-server-all/fireback_arm64_linux.zip
          asset_name: fireback_arm64_linux.zip
          asset_content_type: application/zip
      - name: upload windows zip arm64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-ubuntu/fireback-server-all/fireback_arm64_windows.zip
          asset_name: fireback_arm64_windows.zip
          asset_content_type: application/zip

      - name: upload windows zip amd64
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts-ubuntu/fireback-server-all/fireback_amd64_windows.zip
          asset_name: fireback_amd64_windows.zip
          asset_content_type: application/zip

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Download PSCP.exe
        run: Invoke-WebRequest https://the.earth.li/~sgtatham/putty/latest/w64/pscp.exe -OutFile pscp.exe
      - name: Download wix installer tools
        run: Invoke-WebRequest https://github.com/wixtoolset/wix3/releases/download/wix3141rtm/wix314-binaries.zip -OutFile wix314-binaries.zip
      
      - name: Create thirdparty directory
        run: New-Item -ItemType Directory -Force -Path thirdparty/wix314-binaries

      - name: Unzip file
        run: Expand-Archive -Path wix314-binaries.zip -DestinationPath thirdparty/wix314-binaries
      
      - name: List contents of thirdparty
        run: Get-ChildItem -Path thirdparty
  
  

      # - name: Download Winrar
      #   run: Invoke-WebRequest https://www.win-rar.com/fileadmin/winrar-versions/winrar/winrar-x32-621.exe -OutFile winrar-installer.exe
      # - name: Install winrar
      #   run: .\winrar-installer.exe /S

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.21.1

      - name: Use Node.js 22
        uses: actions/setup-node@v3
        with:
          node-version: 22
      - name: Install the dependencies
        run: cd ui && npm i --force

      - name: Package windows
        run: cd cmd/fireback && make wininstaller

      - uses: actions/upload-artifact@master
        with:
          name: artifacts-windows
          path: artifacts

      ## Test the module builder ./artifacts/fireback/f gen gof --def modules/fireback/WorkspaceModule3.yml --relative-to /Users/ali/work/fireback --gof-module github.com/torabian/fireback
      ## Changes might break them

      # - name: Build System Service
      #   run: cd backend && npm run fb:build:win

      # - name: Package the windows
      #   run: .\cmd\fireback\msi\package-windows.bat
      # - name: Package the software
      #   run: .\backend\cmd\fireback\windows-package.bat

      # - name: Upload artifact
      #   if: ${{ inputs.autodeploy == true }}
      #   if: ${{ inputs.release_artifacts == true }}
      #   run: echo y | .\pscp.exe -pw "${{ secrets.DIGITAL_OCEAN_KEY }}" .\fireback_latest_windows.exe root@pixelplux.com:/var/www/html/fireback-artifacts

  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.22

      # - name: Install deps
      #   run: sudo apt-get update -y && sudo apt install -y sshpass
      - name: Use Node.js 22
        uses: actions/setup-node@v3
        with:
          node-version: 22
      - name: install prettier, so compiler would pick it up
        run: sudo npm i -g prettier
      - name: Install the dependencies
        run: cd ui && npm i --force
      - name: Run go tests
        run: make test
      - name: Build and package deb files
        run: cd cmd/fireback && make deb && make linux-zip
      - name: Windows zip
        run: cd cmd/fireback && make windows-zip
      - name: Install debian file for x64
        run: sudo dpkg -i artifacts/fireback-server-all/fireback-amd64.deb
      # - name: Refresh the fireback dependencies
      #   run: make defs

      # - name: 
      #     Refreshing golang modules should not make a commit.
      #   run: |
      #     git diff --exit-code
      #     if [[ $(git diff --name-only) ]]; then
      #       echo "Changes detected in the following files:"
      #       git diff --name-only
      #       exit 1
      #     fi
      - uses: actions/upload-artifact@master
        with:
          name: artifacts-ubuntu
          path: artifacts
   
    
  build-macos:
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.22

      - name: Install sshpass
        run: brew install hudochenkov/sshpass/sshpass

      - name: Use Node.js 22
        uses: actions/setup-node@v3
        with:
          node-version: 22
      
      - name: Install the dependencies
        run: cd ui && npm i --force
      - name: Download Packages installer build tool
        run: wget http://s.sudre.free.fr/Software/files/Packages.dmg

      - name: Mount Packages image
        run: hdiutil attach Packages.dmg

      - name: Install Packages
        run: sudo installer -pkg /Volumes/Packages\ 1.2.11/Install\ Packages.pkg -target /

      - name: Build Backend
        run: cd cmd/fireback && make mac-pkg

      - uses: actions/upload-artifact@master
        with:
          name: artifacts-darwin
          path: artifacts

  # Packs every ui/packages/* workspace package (see ui/packages/README.md) into a
  # normal npm-installable tarball, so a brand new project can `npm install
  # <url>.tgz` straight from this build's GitHub release instead of vendoring this
  # whole repo as a git submodule. See Makefile's ui-packages-pack target.
  build-ui-packages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js 22
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: Install the dependencies
        run: cd ui && npm i --force

      - name: Pack workspace packages into tarballs
        run: make ui-packages-pack

      - uses: actions/upload-artifact@master
        with:
          name: artifacts-ui-packages
          path: artifacts/fireback-packages

  # Builds src/apps/wasm-demo (see that folder's own doc comments) - a fireback
  # server compiled to wasm, running entirely client-side against an in-browser
  # Postgres (pglite), no network backend at all - and deploys it to GitHub
  # Pages. Only ever runs on a manual workflow_dispatch with the "Deploy docs and
  # demo to pages" input on (never on a plain push/PR), so this is a deliberate,
  # release-time action rather than something that fires on every commit -
  # matching deploy_github_release's own release_artifacts input just above.
  # needs: build-ubuntu so a broken `make test`/Go build blocks the deploy
  # instead of publishing a demo backed by a binary that doesn't even compile.
  deploy_wasm_demo_pages:
    needs:
      - build-ubuntu
    name: deploy_wasm_demo_pages
    if: ${{ inputs.github_pages == true }}
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.22

      - name: Use Node.js 22
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: Install the dependencies
        run: cd ui && npm i --force

      # ui/public/fireback.wasm is gitignored (a real build artifact, several
      # tens of MB) - `make wasm` (cmd/fireback-wasm's own Makefile) compiles it
      # fresh and drops it straight into ui/public, where Vite serves it
      # unmodified. wasm_exec.js (Go's wasm runtime glue) is committed there
      # already, so there's nothing else to fetch before building the app.
      - name: Build fireback.wasm
        run: make wasm
 
      - name: Build the wasm-demo app
        run: cd ui && npm run wasm-demo:github

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ui/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # run-cypress-mysql:
  #   needs:
  #     - build-ubuntu
  #   uses: ./.github/workflows/cypress-test.yml
  #   with:
  #     DB_TYPE: mysql
  
  run-cypress-postgres:
    needs:
      - build-ubuntu
    uses: ./.github/workflows/cypress-test.yml
    with:
      DB_TYPE: postgres

  # run-cypress-sqlite:
  #   needs:
  #     - build-ubuntu
  #   uses: ./.github/workflows/cypress-test.yml
  #   with:
  #     DB_TYPE: sqlite