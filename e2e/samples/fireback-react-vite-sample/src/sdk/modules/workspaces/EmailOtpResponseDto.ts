/*
 *	Generated by fireback 1.2.1
 *	Written by Ali Torabi.
 *	Checkout the repository for licenses and contribution: https://github.com/torabian/fireback
 */
import { BaseDto, BaseEntity } from "../../core/definitions";
import { PublicAuthenticationEntity } from "./PublicAuthenticationEntity";
import { UserSessionDto } from "./UserSessionDto";
// In this section we have sub entities related to this object
// Class body
export type EmailOtpResponseDtoKeys = keyof typeof EmailOtpResponseDto.Fields;
export class EmailOtpResponseDto extends BaseDto {
  public request?: PublicAuthenticationEntity | null;
  requestId?: string | null;
  public userSession?: UserSessionDto | null;
  userSessionId?: string | null;
  public static Fields = {
    ...BaseEntity.Fields,
    requestId: `requestId`,
    request$: `request`,
    request: PublicAuthenticationEntity.Fields,
    userSessionId: `userSessionId`,
    userSession$: `userSession`,
    userSession: UserSessionDto.Fields,
  };
  public static definition = {
    name: "emailOtpResponse",
    fields: [
      {
        name: "request",
        type: "one",
        target: "PublicAuthenticationEntity",
        computedType: "PublicAuthenticationEntity",
        gormMap: {},
      },
      {
        name: "userSession",
        type: "one",
        target: "UserSessionDto",
        computedType: "UserSessionDto",
        gormMap: {},
      },
    ],
  };
}
