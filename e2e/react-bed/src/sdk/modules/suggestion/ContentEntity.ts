/*
*	Generated by fireback 1.2.4
*	Written by Ali Torabi.
*	Checkout the repository for licenses and contribution: https://github.com/torabian/fireback
*/
    import {
        BaseDto,
        BaseEntity,
    } from "../../core/definitions"
// In this section we have sub entities related to this object
// Class body
export type ContentEntityKeys =
  keyof typeof ContentEntity.Fields;
export class ContentEntity extends BaseEntity {
  public children?: ContentEntity[] | null;
  /**
  The content title, such as video title, course title, post title.
  */
  public title?: string | null;
  /**
  Excerpt of the content, useful for search and sometimes showing on different location of the website or app, in lists or feeds.
  */
  public excerpt?: string | null;
  /**
  The content title, such as video title, course title, post title.
  */
  public contentType?: string | null;
  public static Navigation = {
      edit(uniqueId: string, locale?: string) {
          return `${locale ? '/' + locale : '..'}/content/edit/${uniqueId}`;
      },
      create(locale?: string) {
          return `${locale ? '/' + locale : '..'}/content/new`;
      },
      single(uniqueId: string, locale?: string) {
          return `${locale ? '/' + locale : '..'}/content/${uniqueId}`;
      },
      query(params: any = {}, locale?: string) {
          return `${locale ? '/' + locale : '..'}/contents`;
      },
      /**
      * Use R series while building router in CRA or nextjs, or react navigation for react Native
      * Might be useful in Angular as well.
      **/
      Redit: "content/edit/:uniqueId",
      Rcreate: "content/new",
      Rsingle: "content/:uniqueId",
      Rquery: "contents",
  };
  public static definition = {
  "rpc": {
    "query": {}
  },
  "name": "content",
  "features": {},
  "gormMap": {},
  "fields": [
    {
      "name": "title",
      "recommended": true,
      "description": "The content title, such as video title, course title, post title.",
      "type": "string",
      "computedType": "string",
      "gormMap": {}
    },
    {
      "name": "excerpt",
      "recommended": true,
      "description": "Excerpt of the content, useful for search and sometimes showing on different location of the website or app, in lists or feeds.",
      "type": "string",
      "computedType": "string",
      "gormMap": {}
    },
    {
      "name": "contentType",
      "recommended": true,
      "description": "The content title, such as video title, course title, post title.",
      "type": "string",
      "computedType": "string",
      "gormMap": {}
    }
  ],
  "description": "Source of the content which will be indexed by the FTS5 table. Other modules can use this entity to set the content."
}
public static Fields = {
  ...BaseEntity.Fields,
      title: `title`,
      excerpt: `excerpt`,
      contentType: `contentType`,
}
}
