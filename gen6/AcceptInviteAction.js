import { ExecutionContext, createParamDecorator } from '@nestjs/common';
import { fetchx } from 'INTERNAL_SDK_LOCATION';
/**
* Action to communicate with the action acceptInvite
*/


	/**
 * FetchAcceptInviteAction
 */

export class FetchAcceptInviteAction {
  static URL = '/user/invitation/accept';
  static Method = 'post';

  
  	static Axios = (clientInstance: AxiosInstance, config: AxiosRequestConfig<unknown>) =>
		clientInstance
		.requestrequest<unknown, AxiosResponse<unknown>, unknown>(config)

		
	
  
  
	static Fetch = (
		init?: TypedRequestInit<unknown, unknown> | undefined,
		qs?: SignoutQueryParams,
		overrideUrl?: string
	) =>
		fetchx<unknown, unknown, unknown>(
			new URL((overrideUrl ?? FetchSignoutAction.URL) + '?' + qs?.toString()),
			init
		)

		
			.then((res) => res.text())
		

	
		

	
}









	
/**
  * @decription The base class definition for acceptInviteReq
  **/

export class AcceptInviteReq {
	 
		/**
  * @type {string}
  * @description The invitation id which will be used to process
  **/
 invitationUniqueId;
		/**
  * @returns {string}
  * @description The invitation id which will be used to process
  **/
getInvitationUniqueId () { return this[`invitationUniqueId`] }
		/**
  * @param {string}
  * @description The invitation id which will be used to process
  **/
setInvitationUniqueId (value) { this[`invitationUniqueId`] = value; return this; } 
	

	

	/** a placeholder for WebRequestX auto patching the json content to the object **/
	static __jsonParsable;

	
		/**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@AcceptInviteReq.Nest() headers: AcceptInviteReq): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new AcceptInviteReq( request.body );
	},
  );


	
}










/**
 * AcceptInviteHeaders class
 * Auto-generated from Module3Action
 */
export class AcceptInviteHeaders extends Headers {

  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  /**
   * @returns {Record<string, string>}
   * Converts Headers to plain object
   */
  toObject() {
    return Object.fromEntries(this.entries());
  }

  
  /**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@AcceptInviteHeaders.Nest() headers: AcceptInviteHeaders): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new AcceptInviteHeaders(Object.entries(request.headers));
	},
  );

  
}




/**
 * AcceptInviteQueryParams class
 * Auto-generated from Module3Action
 */
export class AcceptInviteQueryParams extends URLSearchParamsX {


  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  
  /**
   * Nest.js decorator for controller query. Instead of using @Query() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@AcceptInviteQueryParams.Nest() query: AcceptInviteQueryParams): string {
   *  return JSON.stringify(query.getMyfield());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new AcceptInviteQueryParams(request.query);
	},
  );

  
}


 
