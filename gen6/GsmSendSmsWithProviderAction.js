import { ExecutionContext, createParamDecorator } from '@nestjs/common';
import { fetchx } from 'INTERNAL_SDK_LOCATION';
/**
* Action to communicate with the action gsmSendSmsWithProvider
*/


	/**
 * FetchGsmSendSmsWithProviderAction
 */

export class FetchGsmSendSmsWithProviderAction {
  static URL = '/gsmProvider/send/sms';
  static Method = 'post';

  
  	static Axios = (clientInstance: AxiosInstance, config: AxiosRequestConfig<unknown>) =>
		clientInstance
		.requestrequest<unknown, AxiosResponse<unknown>, unknown>(config)

		
		.then((res) => {
			return {
			...res,

			
			// if there is a output class, create instance out of it.
			data: new GsmSendSmsWithProviderRes(res.data),
			};
		});
		
	
  
  
	static Fetch = (
		init?: TypedRequestInit<unknown, unknown> | undefined,
		qs?: SignoutQueryParams,
		overrideUrl?: string
	) =>
		fetchx<unknown, unknown, unknown>(
			new URL((overrideUrl ?? FetchSignoutAction.URL) + '?' + qs?.toString()),
			init
		)

		
			.then((res) => res.json())
		

	
		
			.then((data) => new GsmSendSmsWithProviderRes (data));
		

	
}









	
/**
  * @decription The base class definition for gsmSendSmsWithProviderReq
  **/

export class GsmSendSmsWithProviderReq {
	 
		/**
  * @type {GsmProviderEntity}
  * @description 
  **/
 gsmProvider;
		/**
  * @returns {GsmProviderEntity}
  * @description 
  **/
getGsmProvider () { return this[`gsmProvider`] }
		/**
  * @param {GsmProviderEntity}
  * @description 
  **/
setGsmProvider (value) { this[`gsmProvider`] = value; return this; } 
	 
		/**
  * @type {string}
  * @description 
  **/
 toNumber;
		/**
  * @returns {string}
  * @description 
  **/
getToNumber () { return this[`toNumber`] }
		/**
  * @param {string}
  * @description 
  **/
setToNumber (value) { this[`toNumber`] = value; return this; } 
	 
		/**
  * @type {string}
  * @description 
  **/
 body;
		/**
  * @returns {string}
  * @description 
  **/
getBody () { return this[`body`] }
		/**
  * @param {string}
  * @description 
  **/
setBody (value) { this[`body`] = value; return this; } 
	

	

	/** a placeholder for WebRequestX auto patching the json content to the object **/
	static __jsonParsable;

	
		/**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@GsmSendSmsWithProviderReq.Nest() headers: GsmSendSmsWithProviderReq): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new GsmSendSmsWithProviderReq( request.body );
	},
  );


	
}













	
/**
  * @decription The base class definition for gsmSendSmsWithProviderRes
  **/

export class GsmSendSmsWithProviderRes {
	 
		/**
  * @type {string}
  * @description 
  **/
 queueId;
		/**
  * @returns {string}
  * @description 
  **/
getQueueId () { return this[`queueId`] }
		/**
  * @param {string}
  * @description 
  **/
setQueueId (value) { this[`queueId`] = value; return this; } 
	

	

	/** a placeholder for WebRequestX auto patching the json content to the object **/
	static __jsonParsable;

	
		/**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@GsmSendSmsWithProviderRes.Nest() headers: GsmSendSmsWithProviderRes): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new GsmSendSmsWithProviderRes( request.body );
	},
  );


	
}








/**
 * GsmSendSmsWithProviderHeaders class
 * Auto-generated from Module3Action
 */
export class GsmSendSmsWithProviderHeaders extends Headers {

  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  /**
   * @returns {Record<string, string>}
   * Converts Headers to plain object
   */
  toObject() {
    return Object.fromEntries(this.entries());
  }

  
  /**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@GsmSendSmsWithProviderHeaders.Nest() headers: GsmSendSmsWithProviderHeaders): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new GsmSendSmsWithProviderHeaders(Object.entries(request.headers));
	},
  );

  
}




/**
 * GsmSendSmsWithProviderQueryParams class
 * Auto-generated from Module3Action
 */
export class GsmSendSmsWithProviderQueryParams extends URLSearchParamsX {


  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  
  /**
   * Nest.js decorator for controller query. Instead of using @Query() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@GsmSendSmsWithProviderQueryParams.Nest() query: GsmSendSmsWithProviderQueryParams): string {
   *  return JSON.stringify(query.getMyfield());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new GsmSendSmsWithProviderQueryParams(request.query);
	},
  );

  
}


 
