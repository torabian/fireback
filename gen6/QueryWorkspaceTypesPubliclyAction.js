import { ExecutionContext, createParamDecorator } from '@nestjs/common';
import { fetchx } from 'INTERNAL_SDK_LOCATION';
/**
* Action to communicate with the action queryWorkspaceTypesPublicly
*/


	/**
 * FetchQueryWorkspaceTypesPubliclyAction
 */

export class FetchQueryWorkspaceTypesPubliclyAction {
  static URL = '/workspace/public/types';
  static Method = 'get';

  
  	static Axios = (clientInstance: AxiosInstance, config: AxiosRequestConfig<unknown>) =>
		clientInstance
		.requestrequest<unknown, AxiosResponse<unknown>, unknown>(config)

		
		.then((res) => {
			return {
			...res,

			
			// if there is a output class, create instance out of it.
			data: new QueryWorkspaceTypesPubliclyRes(res.data),
			};
		});
		
	
  
  
	static Fetch = (
		init?: TypedRequestInit<unknown, unknown> | undefined,
		qs?: SignoutQueryParams,
		overrideUrl?: string
	) =>
		fetchx<unknown, unknown, unknown>(
			new URL((overrideUrl ?? FetchSignoutAction.URL) + '?' + qs?.toString()),
			init
		)

		
			.then((res) => res.json())
		

	
		
			.then((data) => new QueryWorkspaceTypesPubliclyRes (data));
		

	
}











	
/**
  * @decription The base class definition for queryWorkspaceTypesPubliclyRes
  **/

export class QueryWorkspaceTypesPubliclyRes {
	 
		/**
  * @type {string}
  * @description 
  **/
 title;
		/**
  * @returns {string}
  * @description 
  **/
getTitle () { return this[`title`] }
		/**
  * @param {string}
  * @description 
  **/
setTitle (value) { this[`title`] = value; return this; } 
	 
		/**
  * @type {string}
  * @description 
  **/
 description;
		/**
  * @returns {string}
  * @description 
  **/
getDescription () { return this[`description`] }
		/**
  * @param {string}
  * @description 
  **/
setDescription (value) { this[`description`] = value; return this; } 
	 
		/**
  * @type {string}
  * @description 
  **/
 uniqueId;
		/**
  * @returns {string}
  * @description 
  **/
getUniqueId () { return this[`uniqueId`] }
		/**
  * @param {string}
  * @description 
  **/
setUniqueId (value) { this[`uniqueId`] = value; return this; } 
	 
		/**
  * @type {string}
  * @description 
  **/
 slug;
		/**
  * @returns {string}
  * @description 
  **/
getSlug () { return this[`slug`] }
		/**
  * @param {string}
  * @description 
  **/
setSlug (value) { this[`slug`] = value; return this; } 
	

	

	/** a placeholder for WebRequestX auto patching the json content to the object **/
	static __jsonParsable;

	
		/**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@QueryWorkspaceTypesPubliclyRes.Nest() headers: QueryWorkspaceTypesPubliclyRes): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new QueryWorkspaceTypesPubliclyRes( request.body );
	},
  );


	
}








/**
 * QueryWorkspaceTypesPubliclyHeaders class
 * Auto-generated from Module3Action
 */
export class QueryWorkspaceTypesPubliclyHeaders extends Headers {

  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  /**
   * @returns {Record<string, string>}
   * Converts Headers to plain object
   */
  toObject() {
    return Object.fromEntries(this.entries());
  }

  
  /**
   * Nest.js decorator for controller headers. Instead of using @Headers() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@QueryWorkspaceTypesPubliclyHeaders.Nest() headers: QueryWorkspaceTypesPubliclyHeaders): string {
   *  return JSON.stringify(headers.getContentType());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new QueryWorkspaceTypesPubliclyHeaders(Object.entries(request.headers));
	},
  );

  
}




/**
 * QueryWorkspaceTypesPubliclyQueryParams class
 * Auto-generated from Module3Action
 */
export class QueryWorkspaceTypesPubliclyQueryParams extends URLSearchParamsX {


  // the getters generated by us would be casting types before returning.
  // you still can use .get function to get the string value.
  #getTyped(key, type) {
    const val = this.get(key);
    if (val == null) return null;

    const t = type.toLowerCase();

    if (t.includes('number')) return Number(val);
    if (t.includes('bool')) return val === 'true';
    return val; // string or any other fallback
  }


  
  /**
   * Nest.js decorator for controller query. Instead of using @Query() value: any, now you can use for example:
   * @example
   * @Get()
   * getHello(@QueryWorkspaceTypesPubliclyQueryParams.Nest() query: QueryWorkspaceTypesPubliclyQueryParams): string {
   *  return JSON.stringify(query.getMyfield());
   * }
   */
  static Nest = createParamDecorator(
	(_data, ctx) => {
		// @ts-ignore
		const request = ctx.switchToHttp().getRequest();
		// @ts-ignore
		return new QueryWorkspaceTypesPubliclyQueryParams(request.query);
	},
  );

  
}


 
