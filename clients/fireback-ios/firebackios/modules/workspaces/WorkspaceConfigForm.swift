import SwiftUI
struct WorkspaceConfigForm: View {
    @State private var formModel = WorkspaceConfigEntity()
    var body: some View {
        Form {
            Section {
            }
        }
    }
}