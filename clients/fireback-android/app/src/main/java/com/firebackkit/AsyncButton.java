package com.firebackkit;

import android.content.Context;

public class AsyncButton extends androidx.appcompat.widget.AppCompatButton {

    public AsyncButton(Context context) {
        super(context);
    }
}
